1. `vercor/types.py` and `vercor/dtypes.py` - explicit owner modules for public `RuntimeArray` plus canonical JAX/NumPy array-construction helpers; `DTypePolicy` accepts only the runtime-owned policy (or the active JAX default when omitted), and `Settings` is not a precision protocol
2. `vercor/pytree.py` - shared declarative PyTree mixin for immutable JAX-registered containers
3. `vercor/physics.py` - canonical keyword-only frozen registered `PhysicalConstants` PyTree with 25 documented traced SI physical values, defensive zero-dimensional NumPy-to-immutable-scalar normalization, non-scalar/nonnumeric leaf rejection that retains real scalar JAX tracers, and one private runtime-dtype normalization boundary built on (1, 2)
4. `vercor/physical_constants.py`, `vercor/settings.py`, `vercor/runtime/__init__.py`, `vercor/topology.py`, and `vercor/setups/config.py` - transitional legacy physical-setting metadata and mutable non-precision `Settings`; strict known-setting overrides, explicit `custom={...}` settings, and `SettingSpec` records; sole static precision ownership through immediately validated `RuntimeOptions.dtype`/`DTypePolicy`; public `ExecutionContext`, `RuntimeDriver`, `ExecutionBackend`, `TopologyPolicy`, `TopologyContext`, `ExchangeTopologyPatch`, and `SurfaceMaskPolicy`; and setup-owned `Spinup`, `JAXGCMConfig`, `VerosConfig`, `CAMulatorConfig`, and `JCMLandAtmosphereConfig` built on (1, 3)
5. `vercor/_field_names.py` and `vercor/fields.py` - private shared field-name de-duplication plus canonical public field vocabulary, `ExchangeField` normalization, and `VectorField` owner
6. `vercor/calendar.py` and `vercor/forcing_index.py` - calendar constants, model-calendar datetime values, leap-year logic, month/day conversion, and daily forcing-index policy
7. `vercor/fluxes/vertical_coordinates.py` - hybrid/sigma-coordinate pressure and altitude helpers consuming traced physical values from (3), built on (1, 3)
8. `vercor/fluxes/utilities.py` - JAX-native scalar/array thermodynamic helpers consuming traced physical values from (3), built on (1, 3)
9. `vercor/fluxes/bulk_formula_cesm.py` - JAX-native atmosphere-ocean / atmosphere-ice bulk flux kernels with local JAX-array normalization and canonical traced physical inputs including momentum and air-temperature reference heights built on (1, 3, 8)
10. `vercor/host_arrays.py` - explicit JAX/NumPy host-transfer boundary for non-differentiable adapters, output, and host `int64` NetCDF time coordinates
11. `vercor/setups/_external/_jax_gcm_pytree.py` - adapter-local JAXGCM PyTree leaf transforms and dtype casting helpers built on (1)
12. `vercor/grids.py` - public JAX-friendly `RectilinearGrid` owner with eager validation, keyword-only coordinate construction, PyTree registration, `RectilinearGrid.from_coordinates(...)`, and `RectilinearGrid.uniform(...)` built on (1, 3)
13. `vercor/grid_geometry.py` - rectilinear center-to-edge geometry and grid identity built on (1, 12)
14. `vercor/field_layout.py` - shared canonical grid-field shape validation, component data-field layout validation, and time-last forcing normalization helpers built on (12)
15. `vercor/interpolators/_bilinear_geometry.py`, `_bilinear_weights.py`, `_bilinear_extrapolation.py`, and `bilinear_rectilinear.py` - private spherical geometry, cell-weight, and extrapolation helper owners behind the public JAX-native bilinear scalar/vector interpolation facade built on (1, 3)
16. `vercor/interpolators/conservative_remap_rectilinear.py` - JAX-native conservative scalar remapping runtime with eager overlap preprocessing built on (1, 3, 12, 13)
17. `vercor/_regridders/base.py` and `vercor/regridding.py` - shared private `_BaseRegridder` grid/interpolator/display state, explicit scalar/vector transfer methods, and public `Regridder`/`RegridderFactory` protocols and factory facade built on (12, 13)
18. `vercor/_regridders/bilinear.py` - bilinear regridder wrapper over (15, 17)
19. `vercor/_regridders/conservative.py` - conservative regridder wrapper over (13, 16, 17)
20. `vercor/grid_masks.py` - land/ocean mask construction, rectilinear area mass calculation, and remap-conservation checks built on (1, 12, 13, 19)
21. `vercor/assets.py` - generic asset cache/download/checksum boundary
22. `vercor/forcing_data.py` - canonical NetCDF forcing-file read boundary
23. `vercor/time_selection.py` - model-time-to-year-seconds and periodic interpolation index helpers built on (6)
24. `vercor/_run_order.py` and `vercor/clock.py` - immutable tuple run-order normalization with string rejection (no deps), and coupler clock/timestep iteration helpers built on (6)
25. `vercor/_logging/*.py` and `vercor/jax_logging.py` - private logger configuration, protocol, host-emission, and JAX-callback owners behind the public logging facade, with explicit `LoggerLike` typing ownership, for Python and traced JAX runtime diagnostics
26. `vercor/setups/_time_helpers.py` - shared setup-time timestep validation, lifecycle assignment, spinup logging, forcing-index, and default-field seeding helpers built on (23)
27. `vercor/setups/_lazy_imports.py` and `vercor/setups/__init__.py` - sole public lazy setup export table; private `_data`/`_external` owner packages do not expose parallel facades
28. `vercor/exchanges.py`, `vercor/coupling.py`, `vercor/_runtime/exchange_keys.py`, and `vercor/recipes.py` - public `Exchange` owner with canonical `target`/`fields`/`regrid` names, public `Coupler`/`CouplerSpec` coupling facade, private runtime regrid-key helper, public exchange configuration type aliases, and canonical `*_FIELDS` exchange recipes built on (4, 5, 12, 17, 18, 19)
29. `vercor/setups/_external/jax_gcm_tools.py` - JCM-specific parameter helpers and direct coordinate/terrain/forcing input-data loading built on (1, 7, 8)
30. `vercor/setups/_external/jax_gcm_fields.py` - package-private JCM output-field mapping and surface-temperature forcing helpers built on (1, 7)
31. `vercor/components/contexts.py` - immutable component setup and step context payloads carrying canonical physical constants and the runtime dtype policy, built on (1, 3, 4, 6, 25)
32. `vercor/_runtime/contracts.py` - runtime receive/send contracts, scalar/vector exchange-field flattening, deterministic fan-in rejection by target field, legal receive/send overlap for feedback sequencing, stable exchange mask key names, and contract construction built on (5, 28)
33. `vercor/_runtime/stores.py`, `state.py`, and `__init__.py` - immutable runtime field stores with shape-preserving replacement plus private component-state containers and package ownership built on (1, 2)
34. `vercor/state.py` - public `RunState` and `ComponentState` owners plus shared read-only field resolution for diagnostics/output views and private runtime states built on (1, 2, 12, 32, 33)
35. `vercor/output/__init__.py` and `vercor/components/contracts.py` - the public output/component declaration layer: `OutputConfig`, snapshot declarations, the runtime-checkable structural `Component` protocol, frozen `ComponentSpec` with authoritative `ComponentSpec.lifecycle` ownership, result/transfer contracts, and immutable public mappings. `SnapshotContext.component` and `ComponentSpec.output` form the temporary import strongly connected component that the documented late import in `components/contracts.py` breaks; Task 7 owns separating output declarations from output runtime contexts. The grouped SCC is otherwise built on (1, 3, 5, 6, 12, 25, 28, 31, 34)
36. `vercor/diagnostics/fields.py`, `tables.py`, `plotting.py`, and `__init__.py` - derived diagnostic fields, console tables, optional plotting, and public reexports with runtime field lookup delegated to (34) built on (10, 34)
37. `vercor/components/base.py`, `data.py`, `_adapter.py`, `_contracts.py`, `_callable_wrappers.py`, `_runtime_fields.py`, `runtime_execution.py`, and `setup_validation.py`; plus `vercor/output/_period.py`, `_period_files.py`, `_component_adapter.py`, `_session.py`, `_dataset.py`, and `_netcdf.py` - concrete `CallableComponent`/`DataComponent` convenience adapters, one private `validate_component_contract()` path and declaration-to-binding preparation bridge, scalar/layout/dtype normalization, declared-update and validation-payload ownership, host/scanned execution with stable compiled payload PyTree structure, and the component-aware output support layer. Runtime-view output remains solely at (76). This layer is built on (1, 3, 5, 6, 10, 12, 14, 25, 28, 31, 32, 33, 34, 35)
38. `vercor/setups/_external/jax_gcm_output.py`, `jax_gcm_runtime.py`, `jax_gcm_state.py`, and `jax_gcm.py` - JAXGCM output/runtime/state ownership, `Spinup.enabled`-only initialization, canonical `JCMState`, direct hook binding, and a lightweight factory that defers JCM/Dinosaur imports until invocation built on (1, 3, 4, 11, 26, 29, 30, 34, 37)
39. `vercor/setups/_external/veros_runtime_settings.py` - explicit lazy Veros backend/runtime configuration side-effect boundary
40. `vercor/setups/_external/veros_setup.py` - concrete Veros setup subclass and setup policy built on (39)
41. `vercor/setups/_external/veros_fluxes.py` - side-effect-free Veros-to-VerCOR flux conversion with separate physical-constant and runtime-dtype inputs built on (1, 3, 9)
42. `vercor/setups/_external/veros_state.py` - side-effect-free package-private Veros host-state copy, mutation, named forcing-field container, forcing, and stepping helpers built on (1, 10)
43. `vercor/setups/_external/veros_output.py`, `veros_runtime.py`, `veros_gcm_state.py`, and `veros_gcm.py` - side-effect-free Veros output/runtime/state implementation ownership, `Spinup.enabled`-only lifecycle, and a lightweight invoked factory that alone explicitly configures Veros before loading those implementations built on (1, 4, 6, 10, 25, 37, 39, 40, 41, 42)
44. `vercor/setups/_external/camulator_imports.py` - package-private lazy CREDIT/postblock optional-dependency loading
45. `vercor/setups/_external/camulator_forcing.py` - CAMulator config loading, forcing context, pure forcing cursor calculation, and command-only runtime forcing cursors built on (26, 44)
46. `vercor/setups/_external/camulator_tensors.py` - typed CAMulator tensor-variable indexing, package-private static forcing tensor staging, and JAX-to-Torch transfer helpers
47. `vercor/setups/_external/camulator_contracts.py` and `camulator_fields.py` - lightweight CAMulator runtime field contract ownership, package-private runtime field initialization, forcing prep, and prediction-field mapping with canonical physical inputs built on (1, 3, 7, 10, 46)
48. `vercor/setups/_external/_camulator_wind_filtering.py` and `camulator_wind_filter.py` - private CAMulator wind-mask/kernel/tensor filtering mechanics plus the public configuration and log-and-skip post-processing facade built on (25)
49. `vercor/setups/_external/camulator_stepper.py` - CAMulator state shifting, forcing concatenation, stepping, direct wind-filter facade calls, and optional postblock processing built on (44, 46, 48)
50. `vercor/setups/_external/camulator_output.py` - CAMulator tensor-to-output-variable shaping, average-output path/coordinate/metadata helpers, setup-state direct private output-adapter construction, period-output record delegation and latest-prediction snapshot writing through `_ComponentOutputAdapter`, metadata loading, unsupported CREDIT-output option validation, and direct h5netcdf forecast-increment writing through shared VerCOR output helpers built on (25, 37)
51. `vercor/setups/_external/camulator_init.py` - CAMulator model, transform, forcing, output metadata, init-noise, and stepper initialization built on (44, 46, 49, 50)
52. `vercor/setups/_external/camulator_runtime_settings.py` - CAMulator TensorFlow/OpenMP/MKL invocation-time environment settings boundary
53. `vercor/setups/_external/camulator_land.py` - CAMulator land host-runtime adapter with shared timestep/cursor setup built on (1, 12, 20, 26, 37, 45)
54. `vercor/setups/_external/camulator_runtime.py`, `camulator_gcm_state.py`, and `camulator.py` - CAMulator runtime/state ownership plus a lightweight invoked factory that rejects unsupported spinup before environment/model initialization built on (4, 7, 10, 26, 37, 45, 46, 47, 50, 51, 52)
55. `vercor/setups/_data/assets.py` - concrete ERA/ECMWF setup forcing asset registry built on (21)
56. `vercor/setups/_data/_field_helpers.py` - shared JAX-backed data-adapter positive masks, surface canonicalization, and field normalization helpers built on (1, 14)
57. `vercor/setups/_data/_component_helpers.py` - shared data-component construction helper for time-interpolated forcing adapters built on (12, 23, 26, 37)
58. `vercor/setups/_data/era5_atmosphere.py` - ERA5 atmospheric data component with separate physical-constant and runtime-dtype diagnostics built on (1, 3, 7, 8, 12, 14, 22, 37, 55, 57)
59. `vercor/setups/_data/era5_ocean.py` - ERA5 ocean forcing adapter with canonical layout and masked SST handling built on (12, 14, 22, 37, 55, 56, 57)
60. `vercor/setups/_data/era5_land.py` - ERA5 land forcing adapter with canonical layout and runtime temperature storage built on (12, 14, 22, 37, 55, 57)
61. `vercor/setups/_data/erainterim_ocean.py` - ERA-Interim ocean forcing adapter built on (12, 14, 22, 37, 55, 56, 57)
62. `vercor/setups/_data/jcm_land.py` - JCM land forcing adapter with coordinate conversion and runtime storage built on (1, 12, 20, 37, 56)
63. `vercor/setups/_jcm.py` - `JCMInputs`, public `load_jcm_inputs(...)`, paired JCM setup with optional preloaded inputs, `dataclasses.replace` forcing injection, and private lazy optional JCM factory loading built on (4, 10, 37, 38, 62)
64. `vercor/setups/_slab/atmosphere.py`, `ocean.py`, `land.py`, and `seaice.py` - slab model adapters built on (1, 9, 12, 37)
65. `vercor/_runtime/time.py` - `RuntimeStepInfo` plus host-precomputed daily/monthly runtime step metadata built on (3, 6, 23, 33)
66. `vercor/_runtime/component_state.py`, `field_transfer.py`, `validation.py`, and `state_validation.py` - component runtime state creation, contract prefill, receive/send, time-sliced export selection, declaration checks, exact registered-component-set enforcement, all-component field/hook validation including components outside run order, and configured exchange/regridder/mask validation built on (1, 5, 14, 28, 32, 33, 37, 65)
67. `vercor/_runtime/dispatch_context.py`, `exchange_dispatch.py`, and `driver.py` - destination-grouped static dispatch context construction carrying normalized constants/runtime dtype, destination-specific exchange dispatch, component runtime stepping orchestration, and sent-store priming built on (1, 3, 6, 31, 32, 33, 37, 65, 66)
68. `vercor/_runtime/component_topology.py` - keyed surface-role component lookup for optional surface-mask policy built on (37)
69. `vercor/_runtime/coupler_state.py` and `topology_state.py` - runtime coupler-state assembly plus frozen grouped exchange topology maps with copied read-only mapping views built on (1, 28, 32, 33, 66)
70. `vercor/_runtime/exchange_topology.py`, `topology_policy.py`, `surface_masks.py`, and `topology.py` - duplicate-safe exchange regridder/identity-mask construction using the runtime dtype, uniform public topology-policy adaptation requiring concrete finite numeric/bool topology masks of the target shape with binary `{0, 1}` and fractional `[0, 1]` ranges, temporary-only `SurfaceMaskPolicy` mask construction, and topology orchestration built on (1, 4, 13, 19, 20, 25, 28, 68, 69)
71. `vercor/_runtime/initialization.py` - setup-time normalized-constant context construction, one-shot component declaration-to-binding preparation under the runtime dtype, declaration-based runtime exchange-field validation, and exchange-topology handoff built on (1, 3, 24, 31, 32, 37, 66, 68, 70)
72. `vercor/_runtime/run_context.py`, `progress.py`, and `interrupts.py` - frozen runtime execution context, progress callbacks, and cancellation controller built on (24, 25, 33, 67)
73. `vercor/_runtime/prepared.py` - one frozen post-setup coupling boundary owning runtime-dtype-normalized physical constants and private component bindings, immutable exchange/run order/contracts/topology/dispatch, runtime controls, interrupts, and the existing `validate_configuration()` structural configuration snapshot over component declarations, callable replacement, array identity, field name, shape, and dtype, physical constants, clock, runtime, and topology; lifecycle/spec callables are identity-only, hidden closure/global/default mutable state is outside the supported configuration contract, and prepared reuse does not re-execute setup or materialize/hash author field contents. Reflective snapshot cleanup is explicitly deferred to Task 4's constructor-only assembly work built on (1, 3, 4, 24, 32, 37, 67, 69, 70, 71, 72)
74. `vercor/_runtime/preparation.py` - runtime state creation, supplied-state validation against prepared contracts/topology, and initial sent-store priming through the prepared dispatch context built on (24, 32, 33, 65, 67, 69, 73)
75. `vercor/_runtime/backends.py` and `runner.py` - cohesive compiled/pure-scanned and host-loop execution ownership, unchanged one-shot scans without output, output-enabled compiled chunk/host-session orchestration through (37), strict custom `ExecutionBackend` type and custom-backend result-schema validation against the prepared topology for exact component, store-field, and fractional-mask names, per-field and mask shapes, and component grid shapes—not grid coordinates or identity, mask values, or dtypes—validated host-capable `RuntimeDriver` adaptation through `ExecutionContext`, backend interrupt checkpoints/translation, and thin run-mode selection, compatibility-warning, signal-scope, and delegation ownership built on (4, 37, 65, 66, 67, 72)
76. `vercor/output/_runtime.py` - runtime-view output mask selection/naming, provider-registered external component snapshot output, and coupler-final-output direct h5netcdf boundary through (35, 37) built on (10, 12, 33, 34, 35, 37)
77. `vercor/_runtime/facade.py` - high-level runtime orchestration over the single prepared coupling for state preparation, execution, and final output built on (24, 25, 31, 33, 34, 67, 72, 73, 74, 75, 76)
78. `vercor/coupler.py` - public setup/output facade for `initial_state(*, prefill_missing=True)`, `run()`, and `write_outputs()` built on (25, 28, 31, 37, 77)
79. `examples/` - runnable setup scripts from `vercor.setups`; the ERA5/JCM example supports injected ocean/JCM inputs/clocks plus short and initial-state-only modes, and examples do not discard prepared states
80. `pyproject.toml`, `vercor/py.typed`, `.github/workflows/python-package.yml`, `tests/fixtures/public_plugin/`, and `tests/fixtures/public_plugin_3_0/` - runtime/test dependency separation, PEP 561 artifacts, the transitional VerCOR wheel/sdist plus isolated current component-contract and frozen-3.0 plugin wheels, outside-checkout protocol-first structural-component smoke/strict-mypy proof while the current fixture still uses pre-Task-4 coupling/output facades, intentional frozen-plugin rejection at the removed 3.0 authoring surface, exact artifact validation, and a Python 3.12 quality job enforcing Black, strict flake8, mypy, compileall, full pytest, and the 90% branch-coverage gate
81. `README.md` and `docs/api-architecture-review.md` - public-only quick starts plus the complete VerCOR 3.1.1 public/private ownership, duplication, extension, compatibility, CI, and deferred-4.0 contract built on (1-80)
82. `docs/superpowers/specs/2026-07-13-vercor-4-api-design.md`, `docs/superpowers/plans/2026-07-13-vercor-4-api.md`, `tests/contracts/vercor-3.1.1-public-api.json`, `tests/test_v4_compatibility_baseline.py`, `.github/workflows/python-package.yml`, and `tests/test_distribution_boundaries.py` - approved VerCOR 4 protocol-first design and plan plus the immutable clean-reference 3.1.1 root/owner export and adapter-signature baseline, with non-optional offline wheel construction from an exact-SHA `git archive`, bounded isolated artifact inspection, and a tested full-history quality-job checkout that keeps the pinned commit available in CI built on (80, 81)
