# VerCOR 0.4.0a1 Protocol-First API Rewrite Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deliver the complete, tested, documented, and committed VerCOR
0.4.0a1 protocol-first API rewrite.

**Architecture:** Public declarations normalize once into private immutable
component bindings. A validated workflow produces clock-step plans, the core
splits them at output boundaries, backends execute chunks through one driver,
and one output coordinator handles every provider. Valid 0.3 workflows are
available only from `vercor.compat.v0_3`.

**Tech Stack:** Python 3.12+, JAX, jaxtyping-style annotations, frozen
dataclasses/PyTrees, pytest, mypy, Black, flake8, h5netcdf, flit.

## Global Constraints

- Root `vercor.__all__` is exactly `Clock`, `Coupler`, `Exchange`,
  `RectilinearGrid`, `RunState`, and `RuntimeOptions`.
- Component physics/constants are JAX-traced; dtype/workflow/topology/backend
  policy is static. Never branch in Python on traced values.
- All new production behavior follows TDD: observe the focused test fail for
  the intended missing behavior before editing production code.
- Public functions/classes have type hints and PEP 257 docstrings; frozen
  runtime containers are registered PyTrees.
- No registry, entry-point discovery, Pydantic, fan-in reducer, public prepared
  graph, or fractional-time subcycling.
- `output=None` performs no I/O. One coordinator owns enabled output for every
  backend/setup.
- Primary 0.4 modules contain no legacy aliases. Compatibility is opt-in through
  `vercor.compat.v0_3` and requires explicit import changes.
- Optional JCM/Veros/CAMulator imports remain lazy. CAMulator is not installed
  or pinned during this work.
- Run focused tests, the fast suite, and the full suite before each commit.
- Update `PROGRESS.md` and `DEPENDENCIES.md` after every meaningful unit.

---

### Task 1: Freeze the 0.3.2 baseline and record the approved specification

**Files:**
- Create: `tests/contracts/vercor-0.3.2-public-api.json`
- Create: `tests/test_v0_4_compatibility_baseline.py`
- Include: `docs/superpowers/specs/2026-07-13-vercor-0.4-api-design.md`
- Include: `docs/superpowers/plans/2026-07-13-vercor-0.4-api.md`
- Modify: `PROGRESS.md`, `DEPENDENCIES.md`

**Produces:** An immutable export/signature baseline tied to commit
`9f0b9131c889bed5c1c2d8ded260add3cfef9524`, plus artifact-test helpers capable
of inspecting a wheel built from that reference without treating the later
0.3 fixture as historical evidence.

- [ ] Write a failing test that requires the manifest, exact reference SHA,
  current 0.3.2 version, root exports, canonical owner exports, and public
  signatures used by the 0.3 adapter.
- [ ] Run the focused test and record the expected missing-manifest failure.
- [ ] Generate the static JSON manifest from the checked-out 0.3.2 API and add
  a bounded reference-artifact inspection helper/test.
- [ ] Run the focused, fast, and full suites.
- [ ] Update progress/dependency memory and commit
  `test: freeze VerCOR 0.3.2 compatibility baseline`.

### Task 2: Introduce typed physical constants and single precision ownership

**Files:**
- Create: `vercor/physics.py`, `tests/test_v0_4_physics.py`
- Modify: `vercor/dtypes.py`, component contexts, `vercor/fluxes/`, setup/runtime
  code consuming `Settings`, `PROGRESS.md`, `DEPENDENCIES.md`

**Produces:** `PhysicalConstants`, a frozen registered PyTree with consistently
named fields; setup/step contexts expose `constants`; precision comes only from
`RuntimeOptions.dtype`.

- [ ] Write failing PyTree, gradient, dtype-ownership, naming, and context tests.
- [ ] Verify RED failures are due to absent 0.4 contracts.
- [ ] Implement `PhysicalConstants`, convert flux/runtime/setup consumers, and
  remove core dependence on `Settings.enable_x64` without changing physics.
- [ ] Run focused gradient/physics tests, fast suite, full suite, update memory,
  and commit `refactor!: unify physical configuration and precision`.

### Task 3: Replace component authoring with the protocol-first contract

**Files:**
- Rewrite: `vercor/components/`
- Create: `tests/test_v0_4_component_contracts.py`
- Modify: setup factories and private runtime normalization just enough to
  consume the new contract; update memory docs.

**Produces:** `Component` protocol, `ComponentSpec`, `CallableComponent`,
`DataComponent`, `LifecycleHooks`, `SetupResult`, `TransferPolicy`, contexts,
and step/prefill/validation result contracts. No `ComponentLike`,
`HostComponent`, component `initialize`, `initial_fields`, constructor payload,
or duplicate output/import-policy properties remain in primary 0.4 modules.

- [ ] Write failing tests for structural/callable/data components, setup
  fields/payload, nested callback/type validation, explicit transfer selection,
  host capability, zero seeding, immutable mappings, and undeclared fields.
- [ ] Verify the intended RED failures.
- [ ] Implement the minimal protocol/adapters and one private normalizer/binding
  bridge; remove the old authoring mixin hierarchy.
- [ ] Run focused component/gradient tests, fast/full suites, update memory, and
  commit `refactor!: unify component authoring contracts`.

### Task 4: Make assembly constructor-only and close public module boundaries

**Files:**
- Modify: `vercor/__init__.py`, `vercor/coupler.py`, public facade modules and
  supporting top-level utilities
- Remove primary API: `vercor/coupling.py`
- Create: `tests/test_v0_4_public_api.py`

**Produces:** Six-symbol root, one canonical owner per advanced contract,
constructor-only `Coupler`, explicit `__all__` for every supported public
module, private imports aliased with underscores, and private-only host/PyTree
helpers.

- [ ] Write failing exact-export/signature tests and absence tests for
  `CouplerSpec`, mutators, duplicate reexports, raw interpolator facades, and
  private annotation leaks.
- [ ] Verify RED, then implement the public boundary and migrate callers to
  complete constructor assembly.
- [ ] Run focused boundary/example tests, fast/full suites, update memory, and
  commit `refactor!: simplify the VerCOR public assembly API`.

### Task 5: Add route IDs, regridder capabilities, route topology, and strict state

**Files:**
- Modify: `vercor/exchanges.py`, `vercor/regridding.py`, `vercor/topology.py`,
  `vercor/state.py`, relevant `vercor/_runtime/` route/topology/validation files
- Create: `tests/test_v0_4_routes_and_state.py`

**Produces:** `Exchange(route_id, regridder_factory)`, scalar `Regridder`,
`VectorRegridder`, single-method `TopologyPolicy.build`, route-keyed patches,
strict foreign-state validation, opaque state metadata, and deterministic
fan-in rejection.

- [ ] Write failing tests for default/explicit/colliding IDs, parameterized
  factories, scalar/vector capability, route patches, duplicate producers,
  incoming driver state, returned coordinates/dtypes/masks, and public state
  surface.
- [ ] Verify RED, implement route/topology/state ownership, and migrate recipes.
- [ ] Run focused exchange/regrid/state tests, fast/full suites, update memory,
  and commit `refactor!: add stable route and state contracts`.

### Task 6: Implement workflow plans and chunk-oriented backends

**Files:**
- Rewrite: `vercor/runtime/__init__.py`, runtime runner/backend/driver ownership
- Create: `vercor/_runtime/execution.py`, `tests/test_v0_4_workflows.py`

**Produces:** `WorkflowContext`, `StepPlan`, `ExecutionPlan`,
`SequentialWorkflow`, `ExecutionChunk`, chunk `ExecutionBackend.execute`, and
`RuntimeDriver.run_step`; preserves output-free one-shot JAX scan, host mode,
interrupts, and differentiability.

- [ ] Write failing plan-validation, custom-workflow, custom-backend,
  before-first-dispatch state-validation, host/JAX selection, interruption, and
  JVP/grad tests.
- [ ] Verify RED and implement validated planning/chunk execution.
- [ ] Run focused runtime/gradient tests, fast/full suites, update memory, and
  commit `feat!: add workflow-driven execution backends`.

### Task 7: Unify output providers and the run-level coordinator

**Files:**
- Rewrite: `vercor/output/`
- Modify: runtime execution integration
- Create: `tests/test_v0_4_output_providers.py`

**Produces:** `OutputProvider`, `OutputContext`, `OutputFrame`, `OutputSpec`,
`PeriodOutput`, `OutputTarget`, one immutable accumulator, and one coordinator.
`Coupler.run(..., output=...)` owns all enabled output; primary
`Coupler.write_outputs` is removed.

- [ ] Write failing tests for runtime/native frames, uniform variable
  selection, custom backend plus period output, collision-safe filenames,
  explicit no-I/O, final fields, snapshots, traced-output rejection, and
  immutable accumulation.
- [ ] Verify RED, implement provider normalization/coordinator, and delete
  duplicate accumulators/hidden output markers.
- [ ] Run focused output/runtime tests, fast/full suites, update memory, and
  commit `feat!: unify component output providers`.

### Task 8: Migrate bundled setups, native providers, examples, and installed 0.4 plugin

**Files:**
- Modify: `vercor/setups/`, `examples/`, `README.md`
- Rewrite: `tests/fixtures/public_plugin/`
- Create/update: installed slab/custom-config/plugin smoke tests

**Produces:** All factories return 0.4 components, plugin-owned frozen configs
work by dependency injection, native JAXGCM/Veros/CAMulator output uses ordinary
providers, examples use constructor assembly/route IDs/output targets, and
optional imports remain lazy.

- [ ] Write failing installed default-slab and composed public-plugin tests.
- [ ] Verify RED, migrate slab/data/external adapters and every example.
- [ ] Run setup/example/plugin tests plus JCM/Veros focused integrations,
  fast/full suites, update memory, and commit
  `refactor!: migrate bundled setups to VerCOR 0.4`.

### Task 9: Add opt-in 0.3 compatibility

**Files:**
- Create: `vercor/compat/__init__.py`, `vercor/compat/v0_3/`,
  `tests/fixtures/public_plugin_0_3_1/`, `tests/test_v0_3_compatibility.py`
- Modify: distribution helpers/metadata and migration-oriented missing-name
  messages

**Produces:** Explicit 0.3 adapters for components, settings, coupler recipe and
mutators, runtime options/backends, exchange keyword/topology keys, old output
configuration, and post-run output; all warn and are scheduled for removal in
0.5.0. Primary 0.4 paths remain alias-free.

- [ ] Write failing runtime, warning, strict-mypy, installed-wheel, and removed
  primary-import tests.
- [ ] Verify RED and implement only valid 0.3 compatibility from the frozen
  manifest.
- [ ] Run focused compatibility/distribution tests, fast/full suites, update
  memory, and commit `feat: add opt-in VerCOR 0.3 compatibility`.

### Task 10: Finish architecture review, migration docs, release metadata, and CI

**Files:**
- Rewrite: `docs/api-architecture-review.md`, `DESIGN.md`, `DEPENDENCIES.md`,
  `PROGRESS.md`, README documentation tests
- Create: `CHANGELOG.md`, `docs/migration-0.3-to-0.4.md`, `docs/releasing.md`
- Modify: `pyproject.toml`, `.github/workflows/python-package.yml`

**Produces:** Exact eight-section 0.4 review, complete public/private inventory,
current design/dependencies/progress, runnable migration examples, version
`0.4.0a1`, wheel and installed-sdist tests, Python 3.12/3.13 native/compat
plugin lanes, base/JCM/Veros lanes, and a macOS smoke.

- [ ] Replace phrase/private-layout tests with executable manifests,
  signatures, snippets, artifacts, and behavior contracts.
- [ ] Update all documentation/release metadata and archive historical progress.
- [ ] Run Black, strict flake8, mypy, compileall, fast/full pytest, 90% branch
  coverage, build, installed artifact/plugin smokes, and `git diff --check`.
- [ ] Record final counts and commit `docs: prepare VerCOR 0.4.0a1`.

## Final Acceptance

- All repository checks and public-only installed artifact tests pass.
- The six-symbol root and canonical owner manifests match live signatures.
- One installed plugin composes custom config/factory, structural component,
  regridder, non-empty topology patch, workflow/backend, period/snapshot output,
  and immutable state replacement without private imports.
- Slab, JCM, and Veros workflows pass; CAMulator remains lazy/source-tested.
- Output-free JVP/reverse gradients remain correct.
- No hidden output marker, reflective prepared snapshot, duplicate accumulator,
  private annotation leak, or primary 0.3 alias remains.
- The complete commit series exists; no release is tagged or published.
